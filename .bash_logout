# /etc/skel/.bash_logout

# This file is sourced when a login shell terminates.

# Clear the screen for security's sake.
[ -e ~/.mysql_history ] && rm ~/.mysql_history
[ -e ~/.psql_history ] && rm ~/.psql_history
clear

